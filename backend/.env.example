# =================================
# GOVLI-AI-V2 ENVIRONMENT VARIABLES
# =================================

# Environment
NODE_ENV=development

# Server Configuration
PORT=3000

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=govli_ai
DB_USER=postgres
DB_PASSWORD=your_secure_password_here

# JWT Authentication
# IMPORTANT: Generate a strong secret (64+ characters) for production
# Example: openssl rand -base64 64
JWT_SECRET=your_jwt_secret_here_minimum_64_characters_required_for_production_security
JWT_EXPIRY=8h
JWT_REFRESH_EXPIRY=7d

# Security & CORS
CORS_ORIGIN=http://localhost:8080
FRONTEND_URL=http://localhost:8080
BCRYPT_ROUNDS=12
MAX_LOGIN_ATTEMPTS=5
LOCK_TIME=900000

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# File Upload Configuration
MAX_FILE_SIZE=10485760
ALLOWED_FILE_TYPES=pdf,doc,docx,jpg,jpeg,png,gif,xls,xlsx

# Storage Configuration
STORAGE_TYPE=local
STORAGE_PATH=./uploads

# Email Configuration (Optional - Not yet implemented)
EMAIL_FROM=noreply@govli.ai
# EMAIL_SERVICE=sendgrid
# EMAIL_API_KEY=your_email_api_key_here

# Redis Configuration (Optional - Not yet implemented)
# REDIS_URL=redis://localhost:6379

# AWS S3 Configuration (Optional - Not yet implemented)
# AWS_REGION=us-east-1
# AWS_BUCKET=govli-ai-uploads
# AWS_ACCESS_KEY_ID=your_aws_access_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret_key

# AI Services (Optional - Not yet implemented)
# OPENAI_API_KEY=your_openai_api_key
# ANTHROPIC_API_KEY=your_anthropic_api_key

# =================================
# PRODUCTION DEPLOYMENT NOTES
# =================================
#
# For Render.com deployment:
# 1. Use managed PostgreSQL for DB_HOST, DB_PORT, DB_NAME, DB_USER, DB_PASSWORD
# 2. Generate new JWT_SECRET: openssl rand -base64 64
# 3. Set CORS_ORIGIN to your Vercel frontend URL
# 4. Set NODE_ENV=production
# 5. All environment variables should be set in Render dashboard
#
# For local development:
# 1. Copy this file to .env
# 2. Update all placeholder values
# 3. Never commit .env to version control
